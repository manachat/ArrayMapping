package arraymappring.src;

public class CallChainInterpreter {

    public CallChain parseString(String input){
        StringBuilder operationWord = new StringBuilder(7); //map: 3 letters | filter 6 letters
        int stringIndex = 0;
        while (stringIndex != input.length()) {
            while (input.charAt(stringIndex) != '{') {
                operationWord.append()
            }
        }
    }


}
